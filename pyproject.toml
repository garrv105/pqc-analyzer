[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-v --tb=short --strict-markers -q"
markers = [
    "slow: marks tests as slow",
    "qiskit: marks tests requiring Qiskit",
]

[tool.coverage.run]
source = ["pqc_analyzer"]
omit = ["tests/*", "scripts/*"]

[tool.coverage.report]
show_missing = true
fail_under = 70
